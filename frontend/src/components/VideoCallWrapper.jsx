// VideoCallWrapper - Global video call container
// This component is used to wrap the video call modal
// It can be extended to handle multiple calls or call states

export default function VideoCallWrapper({ children }) {
  return children;
}
